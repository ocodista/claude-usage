# Project Guidelines

## Server Management

- **NEVER** build or restart the server unless explicitly asked by the user
- Do not run `bun run`, `npm run`, or any server start commands automatically
- Only make code changes; let the user handle running/testing
